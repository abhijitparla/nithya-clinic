import React from 'react'

function currency(amount: number, curr: string) {
    return '55'
}

const TyperTick = () => {
  return (
    <div>
      {currency(4,'tt')}
    </div>
  )
}

export default TyperTick
